# Work Orders: <feature>

The build queue. Each work order is its own change (schema: forge-workorder), built one at a time
(`/opsx:apply <wo-id>`), on its own branch/PR.

- [ ] <wo-id> — As a <persona>, I want <capability>  ·  `openspec new change <wo-id> --schema forge-workorder`
- [ ] <wo-id> — …
